# orgh ハンドオフ(2026-08-03 更新)

前身の「残件実装タスク」(HANDOFF タスク0〜7)は**全て実装・コミット済み**。
このファイルは実運用フェーズの引き継ぎ。次セッションはここから読む。

## 現在地

- 実装: タスク0〜7 完了(テスト105件グリーン、`git log` の13コミット)
- 実運用: 実ミッション4本を実行。**完走1・予算超過で死亡2・キャンセル1**
- アウトカム実績: sikore-slot の図柄7種刷新を本番反映(https://sikore-slot.vercel.app)
- 運用config: リポ直下 `config.yaml`(gitignore)。vault=`~/Documents/Obsidian/vault`、inbox=`00-Inbox`、
  worktree有効、parallel 2、**予算は無制限(ユーザー裁定)**
- watch起動: `cd ~/projects/org-harness/ops && ../.venv/bin/orgh --config ../config.yaml watch`
  (opsディレクトリから起動する。リポ直下からだと workdir "." が自己改変ガードに触れる)

## 実運用で判明した最重要問題(次セッションの主題)

**retro が「増幅」ではなく「ノイズ増幅」を始めている。**

`playbooks/coding.md` `playbooks/design.md` に教訓が計4本書かれたが:

1. **4本とも予算上限の話**(「同種タスクは2.0 USD以上に」等)。予算を撤廃したので**全部すでに嘘**
2. 中身が運用トリビアであって組織知ではない
3. 一方 **4ミッション全部で再現している「Plannerの過剰分解」については教訓ゼロ**
   (5行のファイル作成に5タスクDAG、不要な「検収チェックリスト」タスクの生成など)

放置すると次回以降のPlannerに陳腐化した予算アドバイスが注入され続け、真の欠陥は放置される。

## 後続タスク(優先順)

### タスクA: retroプロンプトの是正 + 汚染教訓の除去(最優先)

- `playbooks/coding.md` `playbooks/design.md` の既存4教訓を**削除**(gcの14日を待たない。両ファイルとも未コミット・全行が予算話なのでファイルごと削除でよい)
- `prompts/retro.md` を書き換え:
  - 「直近ミッションの失敗を要約」ではなく**「複数ミッションで繰り返し現れた構造的パターンを抽出せよ」**
  - **禁止事項を明示**: 単発の運用値(予算額・タイムアウト値等)、そのミッション固有の一過性事情、次回に一般化できない記述
  - 「学びがなければ lessons は空文字」の既存指示は維持(無理に書かせない)
- 受け入れ条件: 既存テスト105件がグリーンのまま(retro.mdはテストが内容を検証していないので回帰リスクは低い)

### タスクB: planner.md に粒度の制約を追加

4/4ミッションで再現している過剰分解への対処。`prompts/planner.md` の制約セクションに追加:

- 検収チェックリスト・ブリーフ等の**メタ成果物をタスクにするな**(成果物そのものを作るタスクのみ立てる)
- 1ファイルの作成・編集で済む仕事に複数タスクを割くな
- 既存の「タスクは3〜8個」は下限が高すぎる。**1〜2タスクで足りる仕事はそう計画してよい**旨を明記

### タスクC: 止まっているミッション `7307189e` の resume

sikore-slot のUI改修ミッション(2026-08-03 00:38着火)。t1が旧予算上限で死亡し、後続5タスク(筐体フレームSVG・リール帯・演出CSS・UI統合・検証)がpendingで固まっている。
予算撤廃済みなので `orgh resume 7307189e --retry-failed` でそのまま続行できるはず。
※ タスクA・Bを先にやると、resume時に汚染教訓が注入されずに済む。

### タスクD: 育成サイクルの実測(A〜C後)

2〜3ミッション回してから `orgh report` を見る。差し戻し率・初回合格率の初期データが「増幅が実在するか」の判定材料。
これがユーザーの「実感が湧くか」への回答になる。

## 後回しでよい改善候補(実運用で発見)

- **観測性**: mission.json はタスク完了時にしか保存されないため、実行中は `pending` 表示のまま。ライブの真実は `ledger.jsonl` 側にある。結果ノートにも実行中タスクが見えない
- **REPLAN経緯が結果ノートに出ない**: 今回ユーザーが「なぜチェックリスト作成に1.83 USD?」と混乱した原因。差し戻し理由にREPLAN経緯を併記すべき
- **watch の Ctrl-C** が実行中workerの完了を待つ(SIGINT→procreg.terminate にすべき)
- **doctor に実プロンプト1発の疎通チェック**を追加(codexのフラグ非互換は `--version` では拾えなかった)
- worktree掃除: `orgh cleanup b6503b9a`(検収・マージ済みなので消してよい)

## 環境メモ

- claude 2.1.220 / codex-cli 0.146.0(codexは `--sandbox workspace-write --skip-git-repo-check` が必須)
- **課金**: claude -p はサブスク認証のため実費なし。2026-06-15付の月次クレジット制移行は**公式に一時停止中**。
  再開されたら `budget_usd` を実費上限として復活させること
- codexサンドボックス内ではChromeを起動できない(GUI登録でabort)。puppeteer系の検証はReviewer側で走る
