- 統合済みの教訓
- 「機能しない」という症状に対して原因を単一と決め付けない。今回は独立した2箇所(Planner未接続/setup_budgetのconfig上書き)が同時に断線しており、片方だけ直しても症状が消えず誤診断・再修正のループ(試行回数の増加)を招いた。配線・設定伝播系のバグでは、まず値を設定/上書きしうる全ソースを横断監査してから修正範囲を確定するほうが手戻りが少ない。 <!-- m:26b29bc7 d:2026-08-23 -->
- 設定値の優先順位(どのソースが最終的に勝つか)が絡むバグでは、「動作するか」の確認だけでなく「上書き経路の監査」をタスクスコープに明示的に含めると、修正が局所的な対症療法で終わらず、同種の二重断線を見落としにくくなる。 <!-- m:26b29bc7 d:2026-08-23 -->
