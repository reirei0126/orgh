- 「現状実装の棚卸し/インベントリ」のような分析系タスクにexecutor skillを明示的に割り当てないと、workerがdesigner(設計者)メタskillをそのまま呼び出してしまい、reviewerがJSONとして解釈できず失敗するケースがある。Plannerはこの種のタスクにも具体的なexecutor skill(または出力形式を明示したインラインプロンプト)を割り当てること。 <!-- m:d0d795d4 d:2026-08-03 -->
- 後続の改修計画・実装タスクが未着手のスペック待ちである一方、依存しないアセット制作タスク(リール帯・図柄SVG)は並行して完了できていた。上流タスクの遅延/失敗が下流の独立タスクをブロックしない依存関係設計は今後も維持する。 <!-- m:d0d795d4 d:2026-08-03 -->
- UI/アセット制作系ミッションでは、着手前に「共有仕様(座標系・命名契約など)確定」を独立タスクとして先頭に置き、後続の並列アセット制作タスク群をそれに依存させる分解が手戻りを防ぐ。契約タスクを省いて並列生産から始めると、統合段階で命名・座標の不整合が露呈しやすい。 <!-- m:09957da4 d:2026-08-05 -->
- 複数の上流成果物(アセット群)を束ねて画面に組み込む「統合」タスクや、状態遷移・タイミング制御を伴う「シーケンス実装」タスクは、単体アセット制作タスクより構造的にattempts数が多くなる傾向がある。これらは実ブラウザ確認などのacceptanceを課すこと自体は正しいが、Planner側は最初から複数iterationを要する前提でタスクを設計し、依存点数が多いタスクほど過大に1タスクへ詰め込まず、可能なら「ロジック層」と「アセット結線層」を分けるなど、統合タスクをさらに一段細かく割ることを検討すべき。 <!-- m:09957da4 d:2026-08-05 -->
